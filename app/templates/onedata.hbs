{{#layout-main-menu appModel=model as |section|}}
  {{#if (eq section 'main-menu')}}
    {{main-menu appModel=model}}
  {{else if (eq section 'sidebar')}}
    {{outlet 'sidebar'}}
  {{else if (eq section 'content')}}
    {{outlet 'content'}}
  {{/if}}
{{/layout-main-menu}}
