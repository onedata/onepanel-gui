{{!-- TODO i18n --}}
{{#if emptyCollection}}
  <div class="row">
    <div class="col-xs-12 text-center">
      {{!-- TODO: a link or action to clusters.new route, this should not be hardcoded! --}}
      {{!-- TODO: empty model should be handled generic in two-level sidebar or something --}}
      {{#link-to "onedata.sidebar.content" "clusters" "new"
        class="btn one-component one-default"
      }}
        Create new cluster
      {{/link-to}}
    </div>
  </div>
{{else}}
  <div class="row">
    <div class="col-xs-12">
      {{two-level-sidebar model=model changeResourceId="changeResourceId"}}
    </div>
  </div>
{{/if}}
