{{!-- TODO FIXME change to div class one list item  --}}
<ul class="one-list">
  <li class="one-list-item">
    <div class="item-header">
      {{search-bar search=(action (mut query)) class="light full-width"}}
    </div>
  </li>
  {{#each items as |item|}}
    {{#if (match-query item.label query)}}
      {{!-- TODO try inject item format and make spaces-list generic searchable list --}}
      {{yield item}}
    {{/if}}
  {{/each}}
</ul>
