{{#each items as |item|}}
  {{main-menu-item
    item=item
    itemClicked=(action "itemClicked" bubbles=false)
    isActive=(eq currentItemId item.id)
    isSelected=(eq sidenavItemId item.id)
  }}
{{/each}}
