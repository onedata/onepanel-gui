{{#app-layout changeTab="mainMenuItemChanged" appModel=model as |section|}}
  {{#if (eq section 'sidebar')}}
    {{outlet 'sidebar'}}
  {{else if (eq section 'content')}}
    {{outlet 'content'}}
  {{/if}}
{{/app-layout}}
