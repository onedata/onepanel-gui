{{#login-layout class=(if model.isOnezoneAvailable "has-visit-via-onezone")}}
  {{#if model.adminUserExists}}
    {{login-box authenticationSuccess="authenticationSuccess"}}
  {{else}}
    {{no-admin-box}}
  {{/if}}
{{/login-layout}}
