<label>
  {{field.placeholder}} {{if field.optional (concat " " (t "components.oneFormFields.optional"))}}
</label>
<span class={{concat "form-control form-control-static one-form-field-static " fieldNameClass}}>
  {{#if fakePassword}}
    {{fake-password}}
  {{else}}
    {{value}}
  {{/if}}
</span>
