<thead>
  <tr>
    <th>{{tt this "headers.hosts"}}</th>
    <th class="text-center toggle-column">{{tt this "headers.database"}}</th>
    <th class="text-center toggle-column">{{tt this "headers.clusterWorker"}}</th>
    <th class="text-center toggle-column">{{tt this "headers.clusterManager"}}</th>
    <th class="text-center toggle-column">{{tt this "headers.primaryClusterManager"}}</th>
  </tr>
</thead>
{{!-- TODO validation - highlight cols/rows if not valid --}}
{{!-- TODO use checkboxes values from above --}}
<tbody>
  {{#each hosts as |host|}}
    {{cluster-host-table-row 
      host=host
      isPrimaryClusterManager=(eq primaryClusterManager host.hostname)
      checkboxChanged=(action "checkboxChanged")
      primaryClusterManagerChanged=(action "primaryClusterManagerChanged")
      isReadOnly=isReadOnly
    }}
  {{/each}}
</tbody>
