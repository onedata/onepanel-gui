{{!-- TODO i18n --}}
<thead>
  <tr>
    <th>Hosts</th>
    <th class="text-center toggle-column">Database</th>
    <th class="text-center toggle-column">Cluster Worker</th>
    <th class="text-center toggle-column">Cluster Manager</th>
    <th class="text-center toggle-column">Primary Cluster Manager</th>
  </tr>
</thead>
{{!-- TODO validation - highlight cols/rows if not valid --}}
{{!-- TODO use checkboxes values from above --}}
<tbody>
  {{#each hosts as |host|}}
    {{cluster-host-table-row 
      host=host
      isPrimaryClusterManager=(eq primaryClusterManager host.hostname)
      checkboxChanged=(action "checkboxChanged")
      primaryClusterManagerChanged=(action "primaryClusterManagerChanged")
      isReadOnly=isReadOnly
    }}
  {{/each}}
</tbody>
  