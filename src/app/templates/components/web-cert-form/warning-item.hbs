<li class="warning-item-container alert alert-warning">
  <OneIcon class="warning-icon" @icon="warning" />
  {{#if options.warningText}}
    <span class="warning-text">{{options.warningText}}</span>
  {{/if}}
  {{#if options.warningTip}}
    <OneTooltip
      @title={{options.warningTip}}
      @triggerEvent="hover"
      class="tooltip-lg"
    />
  {{/if}}
</li>
