{{#bs-form submitOnEnter=false class="form-horizontal ceph-main-options-form clearfix" as |form|}}
  {{one-form-fields
    bsForm=form
    fields=currentFields
    formValues=formValues
    inputChanged=(action "inputChanged")
    onFocusOut=(action "focusOut")
    disabled=disabled
  }}
{{/bs-form}}
