<strong>{{tt this "objectStorageDaemon"}}</strong>
{{#if allowsEdition}}
  <span class="space"></span>
  {{one-label-tip title=(tt this "tip")}}
  <span class="space"></span>
  {{remove-icon
    class="remove-osd"
    tooltipText=(tt this "removeOsd")
    onClick=(action removeOsd)}}
{{/if}}
{{#bs-form submitOnEnter=false class="form-horizontal clearfix" as |form|}}
  {{one-form-fields
    layoutConfig=layoutConfig
    bsForm=form
    fields=currentFields
    formValues=formValues
    inputChanged=(action "inputChanged")
    onFocusOut=(action "focusOut")
  }}
{{/bs-form}}
