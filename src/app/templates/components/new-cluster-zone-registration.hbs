{{!-- TODO i18n --}}

{{#bs-form submitOnEnter=false class="form-horizontal" as |form|}}
  {{one-form-fields
    bsForm=form
    fields=fields 
    formValues=formValues 
    inputChanged=(action "inputChanged")
    disabled=_isBusy
  }}

  {{#spin-button
    type="submit"
    class="btn btn-default"
    buttonStyle="expand-left"
    action=(action "submit")
    i18nPrefix="components.newClusterZoneRegistration"
  }}
    {{t 'components.newClusterZoneRegistration.register'}}
  {{/spin-button}}
{{/bs-form}}

