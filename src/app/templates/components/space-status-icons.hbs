{{#status-toolbar as |toolbar|}}
  {{toolbar.icon
    icon="space-import"
    status=importStats.importStatus
    hint=_importHint
    enabled=importEnabled
  }}
  {{toolbar.icon
    icon="update"
    status=importStats.updateStatus
    hint=_updateHint
    enabled=updateEnabled
  }}
{{/status-toolbar}}
